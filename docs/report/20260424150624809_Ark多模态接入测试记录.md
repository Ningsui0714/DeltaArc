# Ark 多模态接入测试记录

时间：2026-04-24 15:06:24

## 结果

已完成火山 Ark Responses API 接入验证，并把应用的 Doubao provider 调整为可直接读取 `ARK_API_KEY`。生产服务已用真实 provider 跑通本地端到端任务，结果为 `provider=doubao`、`model=doubao-seed-2-0-lite-260215`、`fallbackUsed=false`。

## 代码改动

- `server/config.ts`：新增 `ARK_API_KEY` 兼容读取，优先 `DOUBAO_API_KEY`，其次 `ARK_API_KEY`。
- `.env.example`：补充 `ARK_API_KEY=` 占位项，不写入真实密钥。
- `server/lib/preflight/doubaoProvider.ts`：多模态 payload 改为图片在前、文本在后；提高结构化输出 token 上限；接入本地 JSON 修复逻辑。
- `server/lib/preflight/prompt.ts`：补充完整结果 JSON 契约、数量约束和短文本约束。
- `server/config.test.ts`、`server/lib/preflight/doubaoProvider.test.ts`：补充 API key 别名、图片顺序、token 上限和 JSON 修复测试。

## 验证记录

- 裸 Ark `/responses` 调用：成功，状态 `completed`，模型能识别示例图片内容。
- 项目 provider 真实调用：成功，`fallbackUsed=false`，能返回结构化 KOC 推演结果。
- 本地生产 API 端到端：`POST /api/preflight-simulations` 创建任务，轮询完成，`fallbackUsed=false`。
- `npm.cmd run typecheck`：通过。
- `npm.cmd test`：114 项通过。
- `npm.cmd run build`：通过。

## 运行状态

本地服务已重启在 `http://127.0.0.1:5001/`，当前进程使用真实 Ark provider。真实密钥只作为进程环境变量使用，没有写入 `.env.local`、文档或源码。

## 风险与建议

- 真实模型单次结构化推演耗时约 120-150 秒，演示时建议提前跑好或保留 mock 模式作为备用。
- 示例图片是 Ark 文档里的模型能力表，与 KOC 宿舍改造文案不匹配；模型会优先读图并转向 AI 模型选型内容，这是正常多模态行为。
- 后续提交作品时应上传真实封面或内容截图，让推演聚焦作品本身。
