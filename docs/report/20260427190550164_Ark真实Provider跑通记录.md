# Ark 真实 Provider 跑通记录

时间：2026-04-27 19:05:50

## 结果

已使用用户提供的 Ark key 接通真实 `doubao` provider，并通过 Browser Use 在页面内跑通 imagegen 宿舍桌面案例。最终结果为 `completed`，provider 为 `doubao`，没有进入 fallback、mock 或 timeout。

## 关键修复

- `.env.local`
  - 写入 `PREFLIGHT_PROVIDER=doubao`
  - 写入 `ARK_API_KEY`
  - 文件已被 `.gitignore` 忽略，结果汇报不回显完整密钥。
- `server/lib/preflight/doubaoProvider.ts`
  - 新增 `thinking: { type: 'disabled' }`，避免 Ark 在长 JSON 任务中先输出大量 reasoning 导致超时。
  - 新增 `text.format=json_object`，约束返回 JSON object。
  - quick 模式 `max_output_tokens` 从 `7200` 调整为 `4200`。
- `src/pages/PreflightStudioPage.tsx`
  - Browser Use fixture 改用 provider 专用轻量真实图。
- `harness/run-generated-preflight-cases.mjs`
  - 支持 `.jpg/.jpeg` 图片 MIME。
  - 切换到 provider 专用轻量真实图。
- `shared/preflightSimulation.ts`
  - 兼容远端模型把百分比字段返回为 `0.x` 小数的情况。
  - `attentionScore`、`nonRelevantShare`、`cohort.exposureShare`、`confidence.score` 会自动归一化到 `0-100`。
- `shared/preflightSimulation.test.ts`
  - 新增真实模型小数百分比归一化测试。
- `server/lib/preflight/doubaoProvider.test.ts`
  - 更新 quick 模式 token 上限断言。

## 素材优化

新增 provider 专用图片：

- `harness/generated-images/case-01-dorm-desk-cover-imagegen-provider.jpg`：约 219KB
- `harness/generated-images/case-02-campus-ai-demo-imagegen-provider.jpg`：约 79KB
- `public/preflight-fixtures/case-01-dorm-desk-cover-imagegen-provider.jpg`
- `public/preflight-fixtures/case-02-campus-ai-demo-imagegen-provider.jpg`

## 探针验证

- Ark 文本探针：成功，约 6 秒返回。
- Ark vision 最小探针：成功，约 6 秒返回。
- 失败定位：原先 90 秒超时不是 key、网络或模型不可用，而是 Responses API 默认 reasoning 输出拖慢长 JSON 任务。
- 修复方式：关闭 thinking，并启用 JSON object 格式约束。

## Browser Use 最终验证

地址：

```text
http://127.0.0.1:5001/?preflightCase=imagegen-dorm
```

结果：

- 状态：`completed`
- Provider：`doubao`
- fallback：否
- mock：否
- timeout：否
- 图片已载入：`case-01-dorm-desk-cover-imagegen-provider.jpg`
- 页面出现：`预演完成`
- 页面出现：`模拟评论区`
- 页面出现：`涨粉动作`
- 人群分层正常显示：`30% / 45% / 15% / 10%`
- 截图：`harness/output/browseruse-ark-real-dorm-normalized.png`

## 本地验证

```powershell
npm run typecheck
npm test
npm run build
```

结果：

- `npm run typecheck`：通过
- `npm test`：通过，`117 passed / 0 failed`
- `npm run build`：通过
- 本地服务已重启：`http://127.0.0.1:5001/`

## 风险

- 真实 provider 单次页面推演耗时约 79-84 秒，仍偏长；后续可进一步缩短 prompt 或拆成“读图/推演”两段缓存。
- `.env.local` 内含真实 Ark key，只保留本机，不应提交或展示。
